# 爬虫监控平台 架构设计方案

> 基于现有项目（afu、doubao、deepseek、yuanbao 四个爬虫模块）的现状，设计一套可观测、可管理的爬虫监控平台。
> 支持双服务器部署，通过主队列 + 消费者队列实现任务路由与状态同步。

---

## 一、现状分析

### 1.1 现有模块分类

| 分类 | 模块 | 驱动方式 | 账号管理 | 部署服务器 |
|------|------|----------|----------|------------|
| A 类 | afu.py | Selenium + Chrome Profile | Chrome 真实 Profile（account_N 目录） | 服务器 A |
| B 类 | deepseek.py | Playwright + Cookie 文件 | cookies1.json ~ cookies31.json | 服务器 A |
| A 类 | doubao.py | Selenium + Chrome Profile | Chrome 真实 Profile（account_N 目录） | 服务器 B |
| B 类 | yuanbao.py | Playwright + Cookie 文件 | cookies1.json ~ cookies31.json | 服务器 B |

### 1.2 现有流程

```
MySQL ent_data_product_llm_task
        │
        ▼
  DatabaseListener (轮询 600s)
        │
        ▼
  AIModelParallelRunner
   ├── deepseek_run_concurrent  (ThreadPoolExecutor, N cookie 并发)
   └── yuanbao_run_concurrent   (ThreadPoolExecutor, N cookie 并发)

afu.py   ──► 独立 Python 进程，手动触发
doubao.py ──► 独立 Python 进程，手动触发
```

### 1.3 现有痛点

- 无统一入口，afu/doubao 需手动执行
- 日志分散在各进程的 stdout，无聚合
- 告警只有钉钉，无平台级告警面板
- 账号状态（Cookie 是否有效、Profile 是否占用）不可视
- 任务进度只能查数据库，无实时展示
- 无法从平台层面控制启停
- 多服务器部署时任务无统一调度，各服务器相互独立，任务分配无法协调

---

## 二、整体架构目标

```
┌───────────────────────────────────────────────────────────────┐
│                      前端监控面板 (Web UI)                      │
│  任务进度 │ 实时状态 │ 告警中心 │ 账号状态 │ 控制台              │
└─────────────────────────┬─────────────────────────────────────┘
                          │ HTTP / WebSocket
┌─────────────────────────▼─────────────────────────────────────┐
│                    后端 API 服务 (FastAPI)                      │
│  任务管理 │ 日志代理 │ 告警规则 │ 账号管理 │ 队列监控           │
└──────┬────────────┬──────────────────────────────────────────┘
       │            │
       │   ┌────────▼──────────────────────────────────────────┐
       │   │           主队列调度层（Queue Dispatcher）           │
       │   │  轮询 MySQL → 按 LlmKey 路由 → 写入 Redis 子队列   │
       │   │  监听消费者回报 → 聚合主状态 → 写回 MySQL           │
       │   └────────┬───────────────────┬────────────────────┘
       │            │                   │
       │   ┌────────▼────────┐ ┌────────▼────────┐
       │   │   服务器 A       │ │   服务器 B       │
       │   │  queue:afu      │ │  queue:doubao   │
       │   │  queue:deepseek │ │  queue:yuanbao  │
       │   │  Consumer 进程  │ │  Consumer 进程  │
       │   │  本地日志文件    │ │  本地日志文件    │
       │   └────────┬────────┘ └────────┬────────┘
       │            │                    │
       │   ┌────────▼────────────────────▼────────┐
       │   │     腾讯云 CLS Log Topic / Logset     │
       │   │   CLS Agent 采集本地日志并统一检索    │
       │   └──────────────────────────────────────┘
       │
  ┌────▼──────────────────────────────────────────────────────┐
  │                        数据存储层                         │
  │  MySQL（任务表 + 平台状态 + 告警摘要）                    │
  │  Redis（任务队列 + 主状态缓存）│ 腾讯云 CLS（完整日志）    │
  └───────────────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 主队列调度层（Queue Dispatcher）

核心职责：**数据库任务 → 路由分发 → 主状态聚合回写**。
部署位置：独立进程，建议部署在监控平台所在服务器（或任意可访问 MySQL + Redis 的节点）。

```
QueueDispatcher
  ├── DB Poller        ：轮询 MySQL，将「未开始」任务写入 Redis 子队列，同时更新主状态为 queued
  ├── Router           ：根据 LlmKey 将任务路由到对应子队列
  │     LlmKey=afu      → Redis Stream: queue:afu
  │     LlmKey=deepseek → Redis Stream: queue:deepseek
  │     LlmKey=doubao   → Redis Stream: queue:doubao
  │     LlmKey=yuanbao  → Redis Stream: queue:yuanbao
  ├── Master Status    ：维护 Redis Hash 中的任务主状态（见 3.1.1）
  └── Result Listener  ：监听 queue:result，聚合消费者回报，批量写回 MySQL
```

#### 路由规则配置（server_routes.yaml）

```yaml
# 定义服务器与队列的绑定关系
servers:
  server_a:
    host: "192.168.x.x"
    queues:
      - afu
      - deepseek

  server_b:
    host: "192.168.x.x"
    queues:
      - doubao
      - yuanbao

redis:
  host: "192.168.x.x"   # 共享 Redis，所有节点均连此处
  port: 6379
  db: 0

dispatcher:
  poll_interval: 30      # DB 轮询间隔（秒）
  max_queue_size: 500    # 单子队列最大待处理任务数（超出则暂停写入）
  result_batch_size: 50  # Result Listener 每次批量写回 MySQL 的条数
```

#### 3.1.1 主状态（Master Status）生命周期

主状态是任务的统一视图，聚合各消费者的执行反馈，是 MySQL 任务状态的权威来源。

```
pending     MySQL 中存在，尚未被 Dispatcher 写入队列
queued      已写入 Redis 子队列，等待消费者领取
dispatched  消费者已 ACK 领取，即将开始执行
running     消费者执行中，每 30s 发送心跳
completed   消费者执行成功，回报结果写回 MySQL
failed      消费者执行失败，写入失败原因，可进入 retrying
retrying    失败后重入子队列，计数 retry_count（可配置最大次数）
stale       心跳超时，视为消费者失联，自动重新入队
```

状态存储：**Redis Hash**（实时读写） + **MySQL**（持久化，最终一致）

```
# Redis Key 设计
task:status:{task_id}     → Hash { status, server_id, crawler, queued_at,
                                    started_at, updated_at, retry_count, error }
task:heartbeat:{task_id}  → String（TTL 90s，Dispatcher 定时检查，超时置为 stale）
queue:{crawler}           → Redis Stream（各爬虫子队列，含 Consumer Group）
queue:result              → Redis Stream（消费者结果回报）
```

#### 3.1.2 主状态同步流程

```
┌─────────────┐  写入子队列 + 置 queued  ┌──────────────────┐
│  DB Poller  │ ───────────────────────► │ Redis queue:{X}  │
│  MySQL 轮询 │                           └────────┬─────────┘
└─────────────┘                                    │ 消费者 XREADGROUP 领取
       ▲                                           ▼
       │                          ┌────────────────────────────┐
       │                          │  Consumer（服务器 A / B）   │
       │                          │  ACK 确认 → dispatched      │
       │                          │  心跳上报 → running         │
       │                          │  执行完毕 → 写 queue:result  │
       │                          └────────────┬───────────────┘
       │                                       │
       │          ┌────────────────────────────▼─────────────────┐
       │          │            Result Listener                    │
       │          │  读取 queue:result → 更新 Redis 主状态        │
       └──────────│  批量写回 MySQL Status（completed/failed）    │
                  │  触发告警（如 failed / stale）                │
                  └──────────────────────────────────────────────┘
```

---

### 3.2 消费者队列（Server Consumer）

每台服务器部署一个 Consumer 进程，只消费本机负责的子队列，执行完毕后回报结果。

```
ServerConsumer（服务器 A 示例）
  ├── 订阅队列       ：queue:afu, queue:deepseek（Consumer Group 模式）
  ├── 并发控制       ：每个队列维护独立的 ThreadPoolExecutor
  ├── 任务领取       ：XREADGROUP → 写 dispatched 状态
  ├── 心跳上报       ：每 30s 续写 task:heartbeat:{task_id}（TTL 90s）
  ├── 爬虫执行       ：调用本机爬虫进程/函数处理任务
  └── 结果回报       ：写入 queue:result（success / failed + 错误详情）
```

#### 消费者配置（consumer.yaml，各服务器独立部署）

```yaml
# 服务器 A 的 consumer.yaml
server_id: server_a
queues:
  - afu
  - deepseek
concurrency:
  afu: 3          # 最多同时处理 3 个 afu 任务（受 Chrome Profile 数量限制）
  deepseek: 10    # 最多同时处理 10 个 deepseek 任务
heartbeat_interval: 30    # 秒
result_queue: queue:result
retry:
  max_count: 3            # 最大重试次数
  delay: 60               # 重试等待秒数
```

```yaml
# 服务器 B 的 consumer.yaml
server_id: server_b
queues:
  - doubao
  - yuanbao
concurrency:
  doubao: 3
  yuanbao: 10
heartbeat_interval: 30
result_queue: queue:result
retry:
  max_count: 3
  delay: 60
```

#### 任务执行与失联处理

```
正常流程
  XREADGROUP 领取 → ACK → heartbeat TTL 续写 → 执行完毕 → 写 queue:result → XACK

失联检测（Dispatcher 侧）
  每 60s 扫描 task:heartbeat:* → 发现 TTL 过期的 running 任务
  → 置状态为 stale → 重新写入对应子队列（retry_count + 1）
  → 超过 max_count 则置 failed，触发告警
```

---

### 3.3 后端 API 服务（FastAPI）

推荐使用 **FastAPI + Uvicorn**，轻量、异步、自带 OpenAPI 文档。

#### 模块划分

```
platform/
├── main_server.py            # FastAPI 应用入口
├── api/
│   ├── tasks.py              # 任务管理接口
│   ├── logs.py               # 日志摘要与 CLS 跳转接口
│   ├── alerts.py             # 告警查询/配置接口
│   ├── accounts.py           # 账号状态接口
│   ├── control.py            # 爬虫启停控制接口
│   └── queue_status.py       # 队列状态监控接口（新增）
├── services/
│   ├── queue_dispatcher.py   # 主队列调度器（新增）
│   ├── process_manager.py    # 爬虫进程管理
│   ├── log_collector.py      # 日志摘要整理与 CLS 查询参数生成
│   ├── alert_engine.py       # 告警规则引擎
│   └── account_monitor.py    # 账号状态监控
├── models/
│   └── schemas.py            # Pydantic 数据模型
└── store/
    ├── state_db.py           # MySQL 初始化与操作
    └── redis_store.py        # Redis 队列与状态操作（新增）
```

#### 核心接口清单

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/tasks` | GET | 查询任务列表（含状态、进度） |
| `/api/tasks/{id}` | GET | 单任务详情（含主状态、所在队列、执行服务器） |
| `/api/logs/summary` | GET | 查询任务关键事件摘要 |
| `/api/logs/query-link` | GET | 返回腾讯云 CLS 检索参数或控制台跳转信息 |
| `/api/logs/ingest` | POST | 可选，本地开发或后备日志上报入口 |
| `/api/alerts` | GET | 告警列表 |
| `/api/alerts/rules` | GET/PUT | 告警规则配置 |
| `/api/accounts` | GET | 所有账号状态 |
| `/api/accounts/{id}/check` | POST | 手动触发账号健康检查 |
| `/api/crawlers` | GET | 爬虫进程列表及状态 |
| `/api/crawlers/{name}/start` | POST | 启动爬虫 |
| `/api/crawlers/{name}/stop` | POST | 停止爬虫 |
| `/api/crawlers/{name}/restart` | POST | 重启爬虫 |
| `/api/queues` | GET | 所有子队列长度、消费速率、堆积情况 |
| `/api/queues/{name}/pause` | POST | 暂停指定子队列的分发 |
| `/api/queues/{name}/resume` | POST | 恢复指定子队列的分发 |

---

### 3.4 日志收集与展示

#### 日志采集方案

现有代码已经使用 Python `logging` 模块，改造成本低。

**推荐方案：本地文件日志 + 腾讯云 CLS 统一采集 + MySQL 仅保存关键摘要**

```
各爬虫进程 / Consumer / Dispatcher
  │
  ├── logging.FileHandler
  │      └── 写入本机日志文件（按服务/爬虫拆分）
  │
  ├── SensitiveDataFilter
  │      └── 脱敏 cookie / token / profile 路径 / 代理凭据
  │
  ▼
本机日志目录
  ├── D:/crawler_logs/server_a/afu/*.log
  ├── D:/crawler_logs/server_a/deepseek/*.log
  ├── D:/crawler_logs/server_b/doubao/*.log
  └── D:/crawler_logs/server_b/yuanbao/*.log
  │
  ▼
腾讯云 CLS Agent
  └── 采集日志文件并上传到 CLS Topic，支持按 server_id / crawler / task_id 检索

FastAPI log_collector.py
  ├── 写入 alerts 表（告警）
  ├── 写入任务关键事件摘要（可选）
  └── 提供日志跳转信息（CLS 检索条件 / 控制台链接）
```

#### 为什么这样设计

- **完整日志以 CLS 为主**：两台服务器日志天然聚合，适合统一查看和检索
- **MySQL 不再保存全文日志**：避免日志表无限膨胀，也减少平台自己实现全文检索的复杂度
- **平台只保留关键摘要**：例如任务开始、任务完成、失败原因、告警记录，便于任务页快速展示
- **适合新手落地**：先把日志写本地文件，再用 CLS Agent 采集，不需要自己实现复杂的日志上传 SDK、重试和缓冲逻辑

#### 日志字段规范

建议所有日志统一输出 JSON 行格式，至少包含以下字段：

```json
{
  "timestamp": "2026-03-30T10:00:00+08:00",
  "level": "INFO",
  "server_id": "server_a",
  "service": "consumer",
  "crawler": "deepseek",
  "task_id": "123456",
  "account_id": "cookies7.json",
  "event": "task_started",
  "message": "task accepted by consumer"
}
```

字段说明：

- `server_id`：日志来自哪台服务器
- `service`：dispatcher / consumer / crawler / api
- `crawler`：afu / doubao / deepseek / yuanbao
- `task_id`：任务链路标识，便于跨服务检索
- `account_id`：账号标识，禁止写入完整 cookie 内容
- `event`：结构化事件名，例如 `task_queued`、`task_started`、`task_failed`
- `message`：给人看的简短说明

#### 敏感信息脱敏要求

所有写入本地文件并上传 CLS 的日志，必须先经过统一脱敏过滤：

- **禁止记录完整 cookie 内容**
- **禁止记录 token / authorization 头原文**
- **禁止记录完整 Chrome Profile 本地绝对路径**
- **禁止记录代理用户名、密码、完整代理连接串**
- **异常日志中的请求头、页面源码、截图路径若包含敏感字段，必须裁剪或替换**

建议替换规则：

```text
cookie=xxxxx                → cookie=[masked]
Authorization: Bearer xxx   → Authorization: [masked]
D:/afu_real_profiles/...    → profile=[masked_path]
http://user:pass@ip:port    → proxy=[masked]
```

#### 腾讯云 CLS 接入方式

推荐使用 **CLS Agent 采集本地日志文件**，不建议首版直接在代码里调用 CLS SDK。

原因：

- Agent 模式部署更简单，适合双服务器
- 断网、重试、缓冲由 Agent 负责，代码侧无需处理
- 爬虫、Consumer、Dispatcher 共用同一套本地日志格式即可

建议在腾讯云中准备：

- 一个 Logset：`crawler-platform`
- 两个 Topic：
  - `crawler-server-a`
  - `crawler-server-b`

也可以按环境拆分，例如 `prod-server-a`、`prod-server-b`。

#### 平台如何查看日志

平台前端不再承担完整日志存储与全文检索。

推荐保留两种能力：

1. **任务详情页展示关键摘要**
   - 最近状态变更
   - 最近失败原因
   - 最近告警

2. **提供 CLS 检索入口**
   - 显示建议检索条件：`task_id=xxx AND crawler=deepseek`
   - 或保存腾讯云日志控制台跳转链接模板，由前端拼接后打开

#### 本地摘要表建议

如果需要保留任务页内的简要日志，可新增轻量摘要表，而不是继续保存全文日志：

```sql
CREATE TABLE task_event_logs (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id      BIGINT NOT NULL,
    crawler      VARCHAR(32),
    server_id    VARCHAR(32),
    event        VARCHAR(64),
    level        VARCHAR(16),
    message      VARCHAR(255),
    created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_task_time (task_id, created_at)
);
```

用途：

- 展示任务关键节点
- 保存失败摘要
- 支撑告警面板联动

不用于保存海量全文日志。

### 3.4.1 腾讯云 CLS 配置建议

#### CLS 资源规划

```yaml
cls:
  region: ap-guangzhou
  logset: crawler-platform
  topics:
    server_a: crawler-server-a
    server_b: crawler-server-b
  query_fields:
    - timestamp
    - level
    - server_id
    - service
    - crawler
    - task_id
    - account_id
    - event
```

#### 服务器日志目录规划

```text
服务器 A
D:/crawler_logs/server_a/dispatcher/dispatcher.log
D:/crawler_logs/server_a/consumer/consumer.log
D:/crawler_logs/server_a/afu/afu.log
D:/crawler_logs/server_a/deepseek/deepseek.log

服务器 B
D:/crawler_logs/server_b/consumer/consumer.log
D:/crawler_logs/server_b/doubao/doubao.log
D:/crawler_logs/server_b/yuanbao/yuanbao.log
```

#### CLS Agent 采集规则建议

- 服务器 A 的 Agent 只采集 `D:/crawler_logs/server_a/**`
- 服务器 B 的 Agent 只采集 `D:/crawler_logs/server_b/**`
- 每条日志必须带 `server_id` 字段，避免跨机器混淆
- Topic 可按服务器拆分，后续若日志量变大，再按服务拆分 Topic

#### 平台与 CLS 的职责边界

- **CLS**：保存完整原始日志，负责全文检索、时间范围过滤、多机统一查看
- **FastAPI 平台**：展示任务状态、告警、关键事件摘要，并提供 CLS 检索入口
- **MySQL**：只保存任务、告警、账号状态、关键事件摘要，不保存全文日志

---

### 3.5 告警系统

#### 告警触发源

| 告警类型 | 触发条件 | 来源 |
|----------|----------|------|
| 账号连续失败 | 同账号连续错误 ≥ N 次 | Consumer 回报 |
| 爬虫进程崩溃 | 进程意外退出 | Process Manager |
| Cookie 失效 | 登录检查失败 | Consumer 回报 |
| 任务积压 | 子队列长度 > 阈值 | Queue Dispatcher |
| 数据库连接失败 | `get_db_connection` 返回 None | 现有 logger.error |
| 消费者失联 | 心跳超时（stale） | Dispatcher Result Listener |
| 任务长期未完成 | 某任务超过 X 分钟仍为 running | Dispatcher 定时扫描 |

#### 告警通道

```
告警引擎
  ├── 钉钉（现有，保留）── send_dingtalk_message()
  ├── 平台内告警面板（新增）── 写入 alerts 表 + WebSocket 推送
  └── 邮件（可选，后续扩展）
```

#### 告警数据结构

```sql
CREATE TABLE alerts (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    level       VARCHAR(16),   -- WARNING / ERROR / CRITICAL
    crawler     VARCHAR(32),
    server_id   VARCHAR(32),
    account_id  VARCHAR(64),
    alert_type  VARCHAR(64),   -- account_error / process_crash / queue_backlog / stale / ...
    message     TEXT,
    resolved    TINYINT DEFAULT 0,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3.6 账号状态监控

#### A 类账号（Selenium Profile：afu / doubao）

```
账号状态维护
  ├── 状态：idle / running / failed / unknown
  ├── 来源：_profiles_in_use 集合（现有代码已有）
  ├── 暴露：Consumer 通过 HTTP 上报给平台
  └── 展示：profile 目录扫描 + 状态上报
```

#### B 类账号（Playwright Cookie：deepseek / yuanbao）

```
账号状态维护
  ├── 状态：valid / invalid / running / idle
  ├── 来源：cookie 文件有效性检查 + 运行时上报
  └── 展示：cookies1~31 每个 cookie 文件的状态列表
```

#### 账号状态数据结构

```sql
CREATE TABLE account_status (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    crawler       VARCHAR(32),
    server_id     VARCHAR(32),
    account_id    VARCHAR(64),
    status        VARCHAR(16),   -- idle / running / failed / invalid
    last_used_at  DATETIME,
    error_count   INT DEFAULT 0,
    updated_at    DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

---

### 3.7 任务进度看板

数据直接来自现有 MySQL 表 `ent_data_product_llm_task`，无需额外存储。
主状态（Redis）作为实时补充，提供队列位置、执行服务器等动态信息。

#### 展示内容

| 字段 | 说明 |
|------|------|
| 任务ID | ProductLlmTaskId |
| 产品ID | ProductId |
| 模型 | LlmKey（deepseek/yuanbao/afu/doubao）|
| 主状态 | pending/queued/dispatched/running/completed/failed |
| 执行服务器 | server_a / server_b |
| 队列位置 | 当前在子队列中的排队深度 |
| 创建时间 | CreatedTime |
| 更新时间 | UpdatedTime |

---

## 四、数据流向总览

```
┌─────────────────────────────────────────────────────────────────┐
│               爬虫进程（各服务器本地运行）                         │
│  ┌──────────────┐  ┌──────────────────┐  ┌────────────────────┐ │
│  │ 本地文件日志   │  │ 心跳 / 状态上报    │  │ MySQL 写结果       │ │
│  └──────┬───────┘  └────────┬─────────┘  └────────────────────┘ │
└─────────┼───────────────────┼───────────────────────────────────┘
          │                   │
          ▼                   ▼
┌──────────────────┐  ┌──────────────────────────────────────────┐
│ 腾讯云 CLS Agent  │  │     FastAPI 后端 + Queue Dispatcher      │
│ 采集本地日志并上云 │  │  ┌─────────────┐  ┌───────────────────┐ │
└────────┬─────────┘  │  │ 进程管理器   │  │ 主队列调度 + 状态聚合 │ │
         │            │  └─────────────┘  └──────────┬────────┘ │
         ▼            │  ┌─────────────┐             │           │
┌──────────────────┐  │  │ 告警引擎    │    ┌─────────▼────────┐ │
│ 腾讯云 CLS       │  │  └─────────────┘    │ Redis            │ │
│ Logset / Topic   │  └────────────────────│ queue:afu/...    │─┘
│ 统一全文日志检索   │                       │ task:status:*    │
└────────┬─────────┘                       └────────┬─────────┘
         │                                          │
         │                                          ▼
         │                               ┌─────────────────┐
         └──────────────────────────────►│ MySQL           │
                                         │ alerts          │
                                         │ account_status  │
                                         │ task（现有表）  │
                                         │ task_event_logs │
                                         └────────┬────────┘
                                                  │
                                                  ▼
                                         ┌─────────────────────┐
                                         │  前端 Web UI         │
                                         │  状态 + 摘要 + CLS入口 │
                                         └─────────────────────┘
```

---

## 五、改造现有爬虫代码的最小侵入方案

### 5.1 日志接入（改动极小）

```python
import json
import logging
from pathlib import Path

class SensitiveDataFilter(logging.Filter):
    def filter(self, record):
        msg = str(record.msg)
        msg = msg.replace("Authorization:", "Authorization: [masked]")
        msg = msg.replace("cookie=", "cookie=[masked]")
        record.msg = msg
        return True


def build_file_logger(log_file, crawler, server_id, service):
    logger = logging.getLogger(f"{service}.{crawler}")
    logger.setLevel(logging.INFO)

    Path(log_file).parent.mkdir(parents=True, exist_ok=True)

    handler = logging.FileHandler(log_file, encoding="utf-8")
    handler.addFilter(SensitiveDataFilter())
    handler.setFormatter(logging.Formatter('%(message)s'))
    logger.addHandler(handler)
    return logger


def log_event(logger, level, server_id, service, crawler, task_id, account_id, event, message):
    payload = {
        "server_id": server_id,
        "service": service,
        "crawler": crawler,
        "task_id": str(task_id),
        "account_id": account_id,
        "event": event,
        "message": message,
    }
    logger.log(level, json.dumps(payload, ensure_ascii=False))
```

说明：

- 首版只要求写本地文件日志
- 腾讯云 CLS 通过 Agent 采集本地文件，不在代码里直接接 CLS SDK
- 若平台需要显示任务摘要，可在关键节点额外写 `task_event_logs`

### 5.2 任务状态上报（改动极小）

```python
# 在任务执行前后各调一次，通知平台更新主状态
import requests

def report_task_status(platform_url, task_id, status, server_id, account_id=None, error=None):
    try:
        requests.post(f"{platform_url}/api/tasks/{task_id}/status", json={
            "status": status,
            "server_id": server_id,
            "account_id": account_id,
            "error": error,
        }, timeout=2)
    except Exception:
        pass

# 在执行函数中添加调用
report_task_status(PLATFORM_URL, task_id, "running", SERVER_ID, account_id)
# ... 执行爬虫逻辑 ...
report_task_status(PLATFORM_URL, task_id, "completed", SERVER_ID, account_id)
```

### 5.3 心跳上报（Consumer 侧自动）

```python
# Consumer 框架自动处理，爬虫代码无需修改
# consumer.py 内部启动后台线程定期续写 heartbeat TTL
import threading, time, redis

def heartbeat_loop(r, task_id, interval=30):
    while True:
        r.setex(f"task:heartbeat:{task_id}", interval * 3, "alive")
        time.sleep(interval)

t = threading.Thread(target=heartbeat_loop, args=(redis_client, task_id), daemon=True)
t.start()
```

### 5.4 腾讯云日志接入说明（新增）

```text
代码侧：
  爬虫 / Consumer / Dispatcher
    → 写本地 JSON 日志文件
    → 日志写入前先脱敏

部署侧：
  每台服务器安装 CLS Agent
    → 配置采集目录 D:/crawler_logs/server_x/**
    → 上传到对应 Topic

平台侧：
  FastAPI 不直接保存全文日志
    → 仅保存任务摘要、告警、状态
    → 任务详情页提供 CLS 检索入口
```

推荐顺序：

1. 先统一日志格式和目录
2. 再部署 CLS Agent 验证两台服务器都能搜到日志
3. 最后补平台里的 CLS 检索入口


---

## 六、技术选型汇总

| 组件 | 选型 | 理由 |
|------|------|------|
| 任务队列 | Redis Streams (XADD/XREADGROUP) | 轻量、支持消费者组、ACK 机制、无需额外部署 |
| 主状态缓存 | Redis Hash / Key-Value | 低延迟实时状态，最终写回 MySQL |
| 后端框架 | FastAPI + Uvicorn | 异步、轻量、自带 OpenAPI |
| 日志存储 | 腾讯云 CLS（完整日志） + MySQL task_event_logs / alerts（摘要） | 两台服务器统一检索，平台只保留关键摘要，降低实现复杂度 |
| 实时推送 | WebSocket（FastAPI 原生支持）| 前端实时日志/告警 |
| 前端框架 | Vue 3 + Vite + Element Plus | 快速搭建，组件丰富 |
| 告警通道 | 钉钉（现有）+ 平台内面板 | 最小改动，复用现有代码 |
| 账号状态 | MySQL account_status 表 + Redis 缓存 | 持久化 + 低延迟读 |

---

## 七、部署方案

### 7.1 单机部署（当前阶段）

```
服务器 A
├── Redis（队列 + 状态缓存，本机）
├── FastAPI 监控后端（platform/main_server.py）
├── Queue Dispatcher（内嵌在 FastAPI 或独立进程）
├── Consumer A（消费 afu + deepseek 队列）
├── afu.py 爬虫进程
└── deepseek.py / main.py 爬虫进程

服务器 B
├── Consumer B（消费 doubao + yuanbao 队列）
├── doubao.py 爬虫进程
└── yuanbao.py / main.py 爬虫进程

共享
└── MySQL（现有，两台服务器均可访问）
```

### 7.2 Redis 部署位置

- **推荐**：Redis 部署在服务器 A（监控后端所在机器），服务器 B 通过内网 IP 访问
- 服务器 B 的 `consumer.yaml` 中配置 `redis_host: <服务器A内网IP>`
- Redis 无需开公网端口，仅内网互通即可

### 7.3 腾讯云 CLS 部署补充

- 服务器 A、服务器 B 均安装 CLS Agent
- 每台服务器只采集本机日志目录，不跨机读取文件
- 服务器首次上线时，先验证以下三件事：
  - 本地日志文件持续写入正常
  - CLS Topic 中可按 `server_id` 搜到日志
  - 按 `task_id` 可以串起 dispatcher、consumer、crawler 的链路日志
- 若后续需要更细粒度控制，可再增加 Topic 或日志字段，不建议首版就做复杂拆分

---

## 八、实施路线图

### Milestone A：队列调度基础设施（优先级最高）
- [ ] 搭建 Redis，设计 Stream Key 命名规范
- [ ] 实现 Queue Dispatcher（MySQL 轮询 → 分发到子队列）
- [ ] 实现 Consumer（XREADGROUP 领取 → 执行 → 写 result）
- [ ] 实现 Result Listener（读取 result → 更新 MySQL 状态）
- [ ] 实现心跳机制 + 失联重试
- [ ] 部署 Consumer B 到服务器 B

### Milestone B：监控后端基础
- [ ] 搭建 FastAPI 项目骨架
- [ ] 任务管理 API（查询进度、状态）
- [ ] 队列状态 API（各子队列长度、消费速率）
- [ ] 任务关键事件摘要存储
- [ ] 告警引擎（规则判断 + 写库 + 钉钉）

### Milestone C：腾讯云日志接入
- [ ] 统一日志 JSON 字段规范
- [ ] 在爬虫、Consumer、Dispatcher 中写本地日志文件
- [ ] 添加敏感信息脱敏过滤
- [ ] 在两台服务器安装并配置 CLS Agent
- [ ] 验证 CLS 中可按 server_id / crawler / task_id 检索
- [ ] 平台增加 CLS 检索入口

### Milestone D：账号管理与前端页面
- [ ] 账号状态上报接口
- [ ] 搭建 Vue 3 前端项目
- [ ] 首页总览（任务统计卡片、队列状态）
- [ ] 任务看板（分页列表、状态筛选）
- [ ] 告警中心（列表 + 规则配置）
- [ ] 账号状态管理（Cookie/Profile 健康度）
- [ ] 控制台（爬虫启停按钮）

### Milestone E：优化完善
- [ ] Cookie 有效性自动检测
- [ ] 任务积压告警阈值配置化
- [ ] 本地日志自动清理（保留 N 天）
- [ ] 多用户权限（可选）

---

## 九、目录结构建议

```
D:\python\model_code\
├── afu\
├── doubao\
├── deepseek\
├── yuanbao\
├── shared-methods\
├── main.py
│
├── platform\                        # 新增：监控平台
│   ├── main_server.py               # FastAPI 入口
│   ├── crawler_consumer.py          # Consumer 进程入口（各服务器部署）
│   ├── queue_dispatcher.py          # Dispatcher 进程入口
│   ├── consumer.yaml                # 消费者配置（各服务器独立）
│   ├── crawlers.yaml                # 爬虫进程注册表
│   ├── api\
│   │   ├── tasks.py
│   │   ├── logs.py
│   │   ├── alerts.py
│   │   ├── accounts.py
│   │   ├── control.py
│   │   └── queue_status.py
│   ├── services\
│   │   ├── queue_dispatcher.py
│   │   ├── process_manager.py
│   │   ├── log_collector.py
│   │   ├── alert_engine.py
│   │   └── account_monitor.py
│   ├── store\
│   │   ├── state_db.py
│   │   └── redis_store.py
│   ├── models\
│   │   └── schemas.py
│   └── web\
│       └── dist\
│
└── crawler_logs\                    # 新增：本地日志目录，供 CLS Agent 采集
    ├── server_a\
    │   ├── dispatcher\
    │   ├── consumer\
    │   ├── afu\
    │   └── deepseek\
    └── server_b\
        ├── consumer\
        ├── doubao\
        └── yuanbao\
```

---

*文档版本：v2.1 | 生成日期：2026-03-30 | 新增腾讯云 CLS 日志接入方案*
